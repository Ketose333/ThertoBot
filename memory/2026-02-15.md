# 2026-02-15 (curated)

- 커밋 후 즉시 푸시를 기본으로 유지(치명적 영향 변경은 푸시 전 재확인).
- 일괄삭제 실행 후 완료 메시지는 보내지 않음(무출력 기본, 필요 시에만 verbose).
- DM에서 확정한 톤/반응 규칙은 다른 채널에도 즉시 동기화.
- 일반 리마인더는 Discord DM(`user:753783778157264936`)으로, YouTube 신영상 알림은 전용 채널(`channel:1471931748194455807`)로 유지.
- 크론 관리는 `studio/cron_webui.py`를 기준으로 운영(목록/즉시실행/토글/삭제 + 미도착 가능성 진단).
- 크론 전달 대상 표기는 Discord에서 `user:<id>`/`channel:<id>` 형식을 표준으로 사용.
- 이미지 규칙 단일 소스는 `studio/IMAGE_RULES.md`로 유지하고, 메모리 중복 규칙 서술은 최소화.
- 사용자 피드백 반영: 이미지 기본 장면은 장식 거의 없는 집 실내 무드로, 구도 기본값은 정면에 가깝게 조정.
- 사용자 요청 반영: 프롬프트에서 '한태율 실사 인물 사진' 같은 문구를 필수로 두지 않고, 핵심 변화만 쓰는 최소 입력 방식으로 전환.
- 데일리로그는 오늘 날짜만 작성하며, 사용자 명시 지시 없이는 과거 날짜 파일을 생성하지 않음.
- 사용자 지시 반영: 이미지 생성 시 사용자 언급이 없으면 배경 소품은 기본 비허용(plain background 우선).
- 사용자 요청: 생성 전 세션 맥락 룰즈 반영. `studio/IMAGE_RULES.md`에 배경 텍스트/로고 금지, 우드톤 배제, 화이트/그레이 중립광, 인물 비중(60~75%), 각도(정면70:측면30), 프롬프트 최소화 원칙을 명문화.
- 사용자 피드백 반영: `IMAGE_RULES.md`에서 배경 plain 강제 문구 제거. 실내 무드 유지를 위해 기본 소품 1~2개 허용으로 완화.
- 사용자 요청 반영: IMAGE_RULES 강도 소폭 완화(정체성 유지 + 디테일 유연 반영, 장면 규칙은 권장 톤으로 완화).
